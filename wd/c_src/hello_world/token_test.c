defined(), sizeof()
<<=, >>=, ...

++, --, <<, >>
<=, >=, ==, !=
&&, ||
*=, /=, %=, +=, -=, &=, ^=, |=
->, ##

[, ], (, ), {, }, .
&, *, +, -, ~, !, /, %
<, >, ^, |,
?, :, =, ,, #, ;

(nested -               ( expression + with / some.stuff))

___valid_Identifier1234567890___
not-an-identifier

#if defined(FLAGS_WORKED)

#endif